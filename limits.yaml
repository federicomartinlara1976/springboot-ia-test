apiVersion: v1
kind: LimitRange
metadata:
  name: recursos
spec:
  limits:
  - default:
      memory: 1Gi
      cpu: 1
    defaultRequest:
      memory: 256Mi
      cpu: 0.5
    max:
      memory: 2Gi
      cpu: 2
    min:
      memory: 128Mi
      cpu: 0.5
    type: Container
